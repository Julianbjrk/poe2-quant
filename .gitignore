# personal state — regenerated on first run / contains your trade history
quant.db
config.json
once.json
once.err
__pycache__/
*.pyc

# updater scratch — never tracked
.quant_backup/
.quant_update_tmp/
